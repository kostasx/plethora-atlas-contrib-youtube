// Generated by CoffeeScript 1.12.4
var colors, initCommands;

colors = require('colors');

initCommands = function(program) {
  return program.command('youtube').description('YouTube utilities').option('--download-video <video>', 'Download video. <video> can be YouTube URL or ID').option('--output <filepath>', 'Filename and path of the downloaded video').option('--get-caption <videoId>', 'Download video captions.').action(function(options) {
    var YT;
    YT = require('./youtube');
    if (options.getCaption) {
      YT.getCaption({
        id: options.getCaption
      }).then(function(res) {
        return console.log(res.captions);
      });
    }
    if (options.downloadVideo) {
      return YT.downloadVideo({
        video: options.downloadVideo,
        output: options.output || null
      });
    }
  });
};

module.exports = initCommands;
